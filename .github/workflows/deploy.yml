name: Inject API Key

on:
  push:
    branches: [main]

permissions:
  contents: write
  
jobs:
  generate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Generate apiKey.js with secret
        run: |
          mkdir -p modules
          echo "export const API_KEY = '${{ secrets.API_KEY }}';" > modules/apiKey.js

      - name: Commit and push changes
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add modules/apiKey.js
          git commit -m "Inject API key [skip ci]" || echo "No changes to commit"
          git push
